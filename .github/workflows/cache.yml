name: Cache Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: cache-9e1b462

      - name: prime-cache-9e1b462
        run: echo "Cache hit? ${{ steps.cache-step.outputs.cache-hit }}"